#!/usr/bin/env

if [[ -f ~/.profile ]]; then
  source ~/.profile
fi

if [[ $- == *i* && -f ~/.bashrc ]]; then
  source ~/.bashrc
fi

if [ -e /home/tanner/.nix-profile/etc/profile.d/nix.sh ]; then . /home/tanner/.nix-profile/etc/profile.d/nix.sh; fi # added by Nix installer
